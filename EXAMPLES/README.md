# ÐMP Examples

Canonical ÐMP operation examples currently live in [`../EXAMPLES/`](../EXAMPLES/).

The uppercase `EXAMPLES/` directory is retained for backward compatibility with existing links. New examples should prefer the lowercase `examples/` convention used by newer Dogenals protocol packages.

Future cleanup can migrate the JSON files into this directory once downstream links are known.
